@model IEnumerable<FitApp.Models.UserWorkout>

@{
    ViewData["Title"] = "My Workouts";
}

<h1>My Workouts</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workout.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workout.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workout.Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workout.CaloriesBurned)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Workout.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Workout.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Workout.Duration)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Workout.CaloriesBurned)
                </td>
                <td class="text-center">
                    <a asp-action="Details" asp-route-id="@item.Workout.Id" class="btn btn-info btn-sm">
                        <i class="fas fa-info-circle"></i>
                    </a>
                    <a asp-action="Edit" asp-route-id="@item.Workout.Id" class="btn btn-warning btn-sm">
                        <i class="fas fa-edit"></i>
                    </a>
                    <form asp-action="Delete" asp-route-id="@item.Workout.Id" method="post" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this workout?');">
                        <button type="submit" class="btn btn-danger btn-sm">
                            <i class="fas fa-trash-alt"></i>
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
